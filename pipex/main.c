#include <stdio.h>

int	main(int argc, char **argv, char **envp)
{
	//check_args();
	//exec_command();
	char *tab[] = {"je","mange","des","pates"};
	while (tab)
	{
		printf("%s", tab[i]);
	}

	tab += 1
	printf("%s", tab);
	return (0);
}
